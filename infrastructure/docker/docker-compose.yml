services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: code2prod
      POSTGRES_PASSWORD: code2prod
      POSTGRES_DB: code2prod
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
